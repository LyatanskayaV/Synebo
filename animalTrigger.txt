trigger animalTrigger on Animal__c (after insert) {
    Animal__c animal = Trigger.New[0];
    ID i = animal.Id;
         if (i != NULL) {  
        AnimalCallout.makeGetCallout(i);    
    }else System.debug('ID = null');
}